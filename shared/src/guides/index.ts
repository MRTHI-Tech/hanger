/**
 * How to take the photo, in drawings.
 *
 * Both clients ask for the same two photographs under the same §5.4 rules, so
 * both need the same guidance — and the guidance is the part that decides
 * whether a try-on comes back looking like anything. Shared rather than
 * duplicated, for the reason PoseGuide's own note predicted: the phone wanted
 * these the moment it grew a camera.
 */
export {PoseGuide} from './PoseGuide';
export {GarmentGuide} from './GarmentGuide';
